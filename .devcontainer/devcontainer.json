{
  "name": "building-hybrid-search-apps-with-redis",
  "dockerComposeFile": [
    "docker-compose.yml"
  ],
  "service": "workspace",
  "workspaceFolder": "/workspaces/building-hybrid-search-apps-with-redis",
  "remoteUser": "vscode",
  "updateRemoteUserUID": true,
  "runServices": [
    "workspace",
    "rhs-frontend",
    "redis-database",
    "redis-insight"
  ],
  "shutdownAction": "stopCompose",
  "hostRequirements": {
    "cpus": 4,
    "memory": "8gb",
    "storage": "32gb"
  },
  "forwardPorts": [
    "rhs-frontend:80",
    "workspace:8081",
    "redis-insight:5540",
    "redis-database:6379"
  ],
  "portsAttributes": {
    "rhs-frontend:80": {
      "label": "Frontend"
    },
    "workspace:8081": {
      "label": "Backend API"
    },
    "redis-insight:5540": {
      "label": "Redis Insight"
    },
    "redis-database:6379": {
      "label": "Redis"
    }
  },
  "remoteEnv": {
    "REDIS_HOST": "redis-database",
    "REDIS_PORT": "6379"
  },
  "postCreateCommand": "bash .devcontainer/scripts/ensure-cache-permissions.sh vscode && if [ -x ./mvnw ]; then ./mvnw -q -DskipTests dependency:go-offline; fi && riot --version",
  "postStartCommand": "bash .devcontainer/scripts/ensure-cache-permissions.sh vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "vmware.vscode-spring-boot",
        "redhat.vscode-xml",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ]
    }
  }
}
